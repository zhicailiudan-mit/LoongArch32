*** Begin Patch
*** Update File: d:/wanlinc/myproject/func_test/soc_verify/rtl/BPU.v
@@
-    reg  [31:0] ras_stack[7:0];
-    reg  [2:0 ] ptr           ;
-    wire inst_31_26 = ifetch_inst[31:26];
-    wire CALL = (inst_31_26 == 6'h15);
-    wire RET  = (inst_31_26 == 6'h13); 
-    assign if_ras_ptr = ptr;
-    always @(posedge cpu_clk or negedge cpu_rstn) begin
-        if(!cpu_rstn) ptr <= 3'b0;
-        else if (pred_error) ptr <= ex_ras_ptr;
-        else if (CALL)  begin
-            ras_stack[ptr] = PC4;
-            ptr <= ptr + 1;
-        end
-        else if (RET )  ptr <= ptr - 1;
-    end
-    wire B = (inst_31_26 == 6'h14);
-
-    wire direct_target = if_pc + {4{ifetch_inst[9]} + ifetch_inst[9:0], ifetch_inst[25:10], 2'b00};
+    reg  [31:0] ras_stack[7:0];
+    reg  [2:0 ] ptr           ;
+    wire [5:0] inst_31_26 = ifetch_inst[31:26];
+    wire CALL = (inst_31_26 == 6'h15);
+    wire RET  = (inst_31_26 == 6'h13); 
+    assign if_ras_ptr = ptr;
+    wire [31:0] PC4 = if_pc + 32'h4; // 明确定义返回地址
+    // ras top index for reading on RET (ptr points to next free)
+    wire [2:0] ras_top = ptr - 3'b001;
+    always @(posedge cpu_clk or negedge cpu_rstn) begin
+        if(!cpu_rstn) begin
+            ptr <= 3'b0;
+            // 可选：清空堆栈不在此处做具体赋值
+        end
+        else if (pred_error) ptr <= ex_ras_ptr;
+        else if (CALL)  begin
+            ras_stack[ptr] <= PC4;
+            ptr <= ptr + 1;
+        end
+        else if (RET)  ptr <= ptr - 1;
+    end
+    wire B = (inst_31_26 == 6'h14);
+
+    // NOTE: 下面的 direct_target 采用一个通用的 26-bit 偏移符号扩展并左移两位的写法。
+    // 如果你的 ISA 使用不同的立即数格式，请据实修改此处的位域和符号位。
+    wire [25:0] imm26 = ifetch_inst[25:0];
+    wire [31:0] direct_target = if_pc + {{6{imm26[25]}}, imm26, 2'b00};
@@
-    reg  [`BHT_TAG_W-1:0] tag     [`BHT_ENTRY-1:0];
-    reg  [`BHT_ENTRY-1:0] valid;
-    reg  [           1:0] history [`BHT_ENTRY-1:0];
-    reg  [          31:0] target  [`BHT_ENTRY-1:0];
+    reg  [`BHT_TAG_W-1:0] tag     [`BHT_ENTRY-1:0];
+    reg  [`BHT_ENTRY-1:0] valid;
+    reg  [           1:0] history [`BHT_ENTRY-1:0];
+    reg  [          31:0] target  [`BHT_ENTRY-1:0];
@@
-    wire [`BHT_TAG_W-1:0] if_tag = if_pc[31:24];      // IF阶段指令地址标签
-    wire [`BHT_TAG_W-1:0] ex_tag = ex_pc[31:24];      // EX阶段指令地址标签
+    wire [`BHT_TAG_W-1:0] if_tag = if_pc[31:24];      // IF阶段指令地址标签
+    wire [`BHT_TAG_W-1:0] ex_tag = ex_pc[31:24];      // EX阶段指令地址标签
@@
-    wire [          31:0] pc_hash = if_pc ^(if_pc >> 10) ^ (if_pc >> 20);                 // 地址折叠
-    wire [`BHT_IDX_W-1:0] index   = pc_hash[`BHT_IDX_W+1:2];    // 表索引
-    wire   hit         =  (tag[index] == if_tag) && valid[index];  
-    wire   pred_taken  =  (B | RET | CALL) ? 1'b1 : (hit & (history[index][1] == 1));        // 生成预测跳转方向
-    assign pred_target =  (B | CALL) ? direct_target : (RET ? ras_stack[ptr] : (pred_taken ?  target[index] : if_pc + 4)) ;        // 生成预测跳转的目标地址
+    wire [          31:0] pc_hash = if_pc ^(if_pc >> 10) ^ (if_pc >> 20);                 // 地址折叠
+    wire [`BHT_IDX_W-1:0] index   = pc_hash[`BHT_IDX_W+1:2];    // 表索引
+    wire   hit         =  (tag[index] == if_tag) && valid[index];  
+    wire   pred_taken  =  (B | RET | CALL) ? 1'b1 : (hit & (history[index][1] == 1));        // 生成预测跳转方向
+    // RET 应使用堆栈顶部（ptr - 1）作为返回地址
+    wire [31:0] ras_top_target = ras_stack[ras_top];
+    assign pred_target =  (B | CALL) ? direct_target : (RET ? ras_top_target : (pred_taken ?  target[index] : if_pc + 4)) ;        // 生成预测跳转的目标地址
@@
-    wire                  N_SUSPEND = ~pl_suspend    ;
+    wire                  N_SUSPEND = ~pl_suspend    ;
@@
-    wire taken_error  = ex_valid &  ex_is_bj & (ex_pred_taken != real_taken)|// 检测分支跳转方向是否预测错误
-                        ex_valid & ~ex_is_bj & real_taken;         
-   
-    wire target_error = ex_valid & ex_is_bj & (ex_pred_target != real_target) & real_taken;                 // 检测目标地址是否预测错误
+    wire taken_error  = ex_valid &  ex_is_bj & (ex_pred_taken != real_taken) | // 检测分支跳转方向是否预测错误
+                        ex_valid & ~ex_is_bj & real_taken;         
+   
+    wire target_error = ex_valid & ex_is_bj & (ex_pred_target != real_target) & real_taken;                 // 检测目标地址是否预测错误
     assign pred_error = taken_error | target_error;
@@
-    wire add_entry     = ex_valid & ex_is_bj  & ~valid[ex_index] ;     // 判断何种情形需要在BHT和BTB中新增表项
-    wire update_entry  = ex_valid & ex_is_bj  & hit ;     // 判断何种情形需要更新BHT和BTB的现有表项
-    wire replace_entry = ex_valid & ex_is_bj  & (tag[ex_index] !=ex_tag) & valid[ex_index] & ~hit;     // 判断何种情形需要替换BHT和BTB的现有表项
+    // 在 EX 阶段判断命中需基于 EX 的 index/tag
+    wire ex_hit        = (tag[ex_index] == ex_tag) && valid[ex_index];
+    wire add_entry     = ex_valid & ex_is_bj  & ~valid[ex_index] ;     // 判断何种情形需要在BHT和BTB中新增表项
+    wire update_entry  = ex_valid & ex_is_bj  & ex_hit ;     // 判断何种情形需要更新BHT和BTB的现有表项
+    wire replace_entry = ex_valid & ex_is_bj  & (tag[ex_index] != ex_tag) & valid[ex_index] & ~ex_hit;     // 判断何种情形需要替换BHT和BTB的现有表项
@@
-    always @(posedge cpu_clk or negedge cpu_rstn) begin
-        if (!cpu_rstn) begin
-            valid <= {`BHT_ENTRY{1'b0}};
-            for (i = 0; i < `BHT_ENTRY; i = i + 1)
-                history[i] <= 2'b10;
-        end else if(update_entry || add_entry)begin
-                if (add_entry) begin
-                    tag    [ex_index] <= ex_tag;
-                    valid  [ex_index] <= 1'b1;
-                    history[ex_index] <= 2'b10;
-                    target [ex_index] <= real_target;
-                end
-                else if (update_entry) begin
-                    if(real_taken) begin
-                        if (history[ex_index] != 2'b11) history[ex_index] <= history[ex_index] + 1;
-                    end else begin 
-                        if (history[ex_index] != 2'b00) history[ex_index] <= history[ex_index] - 1;
-                    end
-                    
-                    if((pred_taken == 0 && real_taken == 1) ||
-                        (pred_taken == 1 && real_taken == 1 && pred_target != real_target)) begin
-                            target[ex_index] <= real_target;
-                    end        
-
-                end
-                else if (replace_entry) begin
-                    tag    [ex_index] <= ex_tag;
-                    valid  [ex_index] <= 1'b1;
-                    history[ex_index] <= 2'b10;
-                    target [ex_index] <= real_target;
-                end
-
-        end
-    end
+    always @(posedge cpu_clk or negedge cpu_rstn) begin
+        if (!cpu_rstn) begin
+            valid <= {`BHT_ENTRY{1'b0}};
+            for (i = 0; i < `BHT_ENTRY; i = i + 1) begin
+                history[i] <= 2'b10;
+                tag[i]     <= {`BHT_TAG_W{1'b0}};
+                target[i]  <= 32'h0;
+            end
+        end else if (ex_valid & ex_is_bj) begin
+                if (add_entry) begin
+                    tag    [ex_index] <= ex_tag;
+                    valid  [ex_index] <= 1'b1;
+                    history[ex_index] <= 2'b10;
+                    target [ex_index] <= real_target;
+                end
+                else if (update_entry) begin
+                    if(real_taken) begin
+                        if (history[ex_index] != 2'b11) history[ex_index] <= history[ex_index] + 1;
+                    end else begin 
+                        if (history[ex_index] != 2'b00) history[ex_index] <= history[ex_index] - 1;
+                    end
+                    // 使用送到 EX 的预测信号进行比较
+                    if((ex_pred_taken == 1'b0 && real_taken == 1'b1) ||
+                       (ex_pred_taken == 1'b1 && real_taken == 1'b1 && ex_pred_target != real_target)) begin
+                            target[ex_index] <= real_target;
+                    end        
+
+                end
+                else if (replace_entry) begin
+                    tag    [ex_index] <= ex_tag;
+                    valid  [ex_index] <= 1'b1;
+                    history[ex_index] <= 2'b10;
+                    target [ex_index] <= real_target;
+                end
+        end
+    end
*** End Patch